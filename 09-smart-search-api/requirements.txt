# --- Web framework ---
fastapi==0.115.6
uvicorn[standard]==0.34.0
pydantic==2.10.4
pydantic-settings==2.7.1

# --- Vector index (persistent FAISS + SQLite docstore, not ephemeral Chroma —
# see app/store.py for why) ---
faiss-cpu==1.9.0.post1
numpy==2.2.1

# --- Embedding providers (all optional at runtime; "fastembed" is the
# zero-API-key default — a small local ONNX model) ---
openai==1.59.6
google-generativeai==0.8.3
fastembed==0.8.0
