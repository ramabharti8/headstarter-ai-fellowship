version: "3.9"

services:
  api:
    build: .
    ports:
      - "8000:8000"
    volumes:
      - ./saved_models:/app/saved_models:ro
    environment:
      - PYTHONUNBUFFERED=1
    restart: unless-stopped
