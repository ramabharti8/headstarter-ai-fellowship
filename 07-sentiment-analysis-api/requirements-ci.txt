# Slim install for CI / offline tests: the app imports torch + transformers
# lazily (only the real backend needs them), so fake-mode tests run without the
# multi-hundred-MB ML stack.
fastapi==0.115.6
uvicorn[standard]==0.34.0
pydantic==2.10.4
pydantic-settings==2.7.1

pytest==8.3.4
httpx==0.28.1
ruff==0.8.4
